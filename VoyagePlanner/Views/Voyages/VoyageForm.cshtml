@model VoyagePlanner.Models.DTOs.VoyageDTO

@{
    ViewData["Title"] = "VoyageForm";
    var myObjectName = ViewBag.destination;
}

<h1>VoyageForm</h1>

<h4>Voyage</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="VoyageHandler">
            <div class="form-group">
                <select asp-for="TravelType" class="form-select" aria-label="Default select example">
                    <option selected class="form-control">Open this select menu</option>
                    @{
                        foreach (string travelType in ViewBag.travelTypes)
                        {
                            <option value="@travelType" class="form-control">@travelType</option>
                        }
                    }
                </select>
            </div>

            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StartDate" class="control-label"></label>
                <input asp-for="StartDate" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EndDate" class="control-label"></label>
                <input asp-for="EndDate" class="form-control" />
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input asp-for="DestinationId" hidden value="@ViewBag.destination.Id" class="form-control" />

            </div>

            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
        @*using (Html.BeginForm("Order", "Home", FormMethod.Post))
        {
        <tr>
            <td>
                <input type="submit" value="Order" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.StartDate)
                <input type="hidden" value="@Model.StartDate" name="StartDate" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.EndDate)
                <input type="hidden" value="@Model.StartDate" name="StartDate" />
            </td>
            <td>
                @Html.DisplayFor(model => Model.Destination, null, "InputQty")
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.TravelType)
            </td>
        </tr>
        }*@
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
